# CHANGE-042 验证报告

日期：2026-05-23

---

## 验证 1：Bug 修复正确性

### 1.1 `get_chapter` 返回字典包含 `novel_id`

文件：`src/api/services/novel_manager.py`，第 319 行

```python
return {"id": c.id, "novel_id": c.novel_id,
        "chapter_number": c.chapter_number,
        ...}
```

**结论：通过。** `"novel_id": c.novel_id` 已正确包含在返回字典中。

### 1.2 `update_chapter` 查询包含 `.order_by(Chapter.id.desc()).limit(1)`

文件：`src/api/services/novel_manager.py`，第 329-333 行

```python
result = await session.execute(
    select(Chapter).where(
        Chapter.novel_id == novel_id,
        Chapter.chapter_number == chapter_number
    ).order_by(Chapter.id.desc()).limit(1)
)
```

**结论：通过。** 查询已包含 `.order_by(Chapter.id.desc()).limit(1)`，与 `get_chapter` 保持一致，防止重复章节号时取到错误行。

### 1.3 `ChapterResponse` 包含 `novel_id` 字段

文件：`src/api/models/responses.py`，第 135-145 行

```python
class ChapterResponse(BaseModel):
    novel_id: str
    chapter_number: int
    ...
```

**结论：通过。** `novel_id: str` 为必填字段，`get_chapter` 返回的字典包含该键，序列化不会报 ValidationError。

### 1.4 路由 `response_model` 声明

文件：`src/api/routes/projects.py`，第 569 行

```python
@router.get("/{novel_id}/chapters/{chapter_number}", response_model=ChapterResponse)
```

**结论：通过。** 路由已声明 `response_model=ChapterResponse`，FastAPI 会用 `ChapterResponse` 对 `get_chapter` 的返回值进行序列化验证。

---

## 验证 2：测试运行结果

命令：`python -m pytest tests/ -k "chapter" -v --tb=short`

| 指标 | 数值 |
|------|------|
| 选中测试数 | 104 |
| 通过 | 103 |
| 失败 | 1 |
| 耗时 | 19.57s |

### 失败测试分析

```
FAILED tests/unit/test_change029_volume_number.py::TestGetChapterVolumeNumber::test_get_chapter_dict_contains_all_expected_keys
```

**原因：** 该测试写于 CHANGE-029，断言 `get_chapter` 返回字典的键集合为旧的 8 个键（不含 `novel_id`）。CHANGE-042 修复后字典新增了 `novel_id`，导致该旧测试的 `expected_keys` 断言失败。

**影响评估：** 这是一个**测试用例过时**问题，不是代码回归。修复行为本身是正确的——`novel_id` 应当包含在返回字典中以满足 `ChapterResponse` 序列化需求。需要更新该测试的 `expected_keys`，将 `"novel_id"` 加入集合。

---

## 验证 3：压测脚本语法检查

命令：`python -m py_compile` 对以下 6 个文件逐一检查

| 文件 | 结果 |
|------|------|
| `loadtest/locustfile.py` | 通过 |
| `loadtest/config.py` | 通过 |
| `loadtest/tasks/chapter_read.py` | 通过 |
| `loadtest/tasks/chapter_list.py` | 通过 |
| `loadtest/tasks/chapter_generate.py` | 通过 |
| `loadtest/tasks/ai_rewrite.py` | 通过 |

所有文件无语法错误。

### `loadtest/data/seed.json` JSON 合法性

命令：`python -c "import json; json.load(open('loadtest/data/seed.json', encoding='utf-8'))"`

**结论：通过。** JSON 格式合法，包含 `novel_ids` 和 `chapter_numbers` 两个必要字段（当前为占位符值，需在实际压测前替换为真实数据）。

---

## 总体结论

| 验证项 | 状态 |
|--------|------|
| `get_chapter` 返回 `novel_id` | 通过 |
| `update_chapter` 使用 `order_by + limit(1)` | 通过 |
| `ChapterResponse.novel_id` 字段定义 | 通过 |
| 路由 `response_model` 声明 | 通过 |
| 章节相关测试（103/104） | 基本通过 |
| 压测脚本语法检查（6 文件） | 通过 |
| `seed.json` JSON 合法性 | 通过 |

**总体结论：需小幅修复后通过。**

唯一问题是 `tests/unit/test_change029_volume_number.py` 中的 `test_get_chapter_dict_contains_all_expected_keys` 测试用例过时——其 `expected_keys` 集合需补充 `"novel_id"`。这不是代码 bug，是旧测试未随修复同步更新。建议在合并前修复该测试。
