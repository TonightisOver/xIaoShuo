# 代码检查报告: 自定义文风（LLM 生成）

**变更 ID**: CHANGE-014  
**版本**: v1  
**检查时间**: 2026-05-16

## 检查范围

后端 Python（API + LangGraph 节点）、前端 Vue、数据库迁移。

## 检查结果

### 通过项

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | get_style_instruction 逻辑正确，预设/自定义分支清晰 | PASS |
| 2 | validate_writing_style 正确接受"自定义"值 | PASS |
| 3 | 5 个 LangGraph 节点统一使用 get_style_instruction | PASS |
| 4 | style.py 端点输入验证（min_length=2, max_length=500） | PASS |
| 5 | DB 迁移 upgrade/downgrade 对称 | PASS |
| 6 | NovelState TypedDict 添加 writing_style_prompt 字段 | PASS |
| 7 | 前端 generateStyle 错误处理（finally 块恢复状态） | PASS |
| 8 | 前端 canSubmit 逻辑不受自定义风格影响 | PASS |

### 发现问题

| # | 严重度 | 文件 | 问题 | 建议 |
|---|--------|------|------|------|
| 1 | 低 | `src/api/routes/style.py` | generate_style 未对 description 做 prompt injection 检测（validate_idea 有此逻辑） | 可复用 suspicious_patterns 检查，当前风险低因为输出仅作为指令模板 |
| 2 | 低 | `frontend/src/views/Create.vue` | generateStyle 失败时无用户提示（仅 finally 恢复按钮状态） | 建议 catch 中设置 error 提示 |
| 3 | 信息 | `src/core/langgraph/nodes/*.py` | style_instruction 注入方式为字符串拼接前缀，若 prompt 为空字符串则多一个换行 | 已有 `if style_instruction:` 守卫，无实际影响 |

### 代码风格

- 命名规范：符合项目现有 snake_case 风格
- 导入顺序：符合 isort 规范
- 类型注解：get_style_instruction 参数和返回值有类型标注

## 结论

代码质量良好，无阻塞性问题。2 个低优先级建议可后续迭代处理。
