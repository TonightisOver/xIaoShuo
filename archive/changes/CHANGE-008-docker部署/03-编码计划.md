# 编码计划

## 变更编号
CHANGE-008

## 任务分解

| # | 任务 | 产出文件 | 验证方式 |
|---|------|----------|----------|
| 1 | 编写 .dockerignore | `.dockerignore` | 确认排除 node_modules、__pycache__、.git |
| 2 | 编写 multi-stage Dockerfile | `Dockerfile` | `docker build` 成功 |
| 3 | 编写环境变量模板 | `.env.docker` | 变量完整覆盖数据库/API 配置 |
| 4 | 编写 Nginx 配置 | `nginx.conf` | 语法检查通过 |
| 5 | 编写 docker-compose 编排 | `docker-compose.yml` | `docker-compose config` 无报错 |
| 6 | 编写一键部署脚本 | `deploy.sh` | shellcheck 无严重警告 |
| 7 | 编写部署指南页面 | `deploy-guide.html` | 浏览器可正常渲染 |

## 依赖关系

```
.dockerignore → Dockerfile → docker-compose.yml
.env.docker → docker-compose.yml
nginx.conf → docker-compose.yml
docker-compose.yml → deploy.sh
```

## 预计工作量

约 2 小时编码 + 1 小时联调部署。
