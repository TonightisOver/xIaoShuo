# 集成测试报告 v1 - CHANGE-011 按卷生成

## 测试范围

端到端流程验证：大纲生成 → volumes 持久化 → 按卷生成章节 → API 查询

## 测试场景

| 场景 | 描述 | 结果 |
|------|------|------|
| 完整流程 | 生成含 volumes 的大纲 → 保存 → 按卷生成 → 查询 | PASS |
| 兼容流程 | 旧格式大纲 → 保存 → 按章生成 → 查询 | PASS |
| API 联调 | 前端调用 volumes API → 返回正确数据 | PASS |
| 前端构建 | frontend build 无报错 | PASS |

## 接口联调

- GET /projects/{id}/volumes/{num}: 返回 volume 详情含 outline JSON
- PUT /projects/{id}/volumes/{num}: 更新 title/summary 成功
- POST /projects/{id}/generate-volume: 返回 202，后台任务启动

## 数据一致性

- volumes.chapter_start/chapter_end 与实际生成章节数对应
- chapters.volume_number 与所属 volume 一致

## 结论

集成测试通过，前后端联调正常，数据一致性验证通过。
