# 编码报告: 人机交互升级

**变更 ID**: CHANGE-020  
**版本**: v1  
**创建时间**: 2026-05-17

## 修改文件

| 文件 | 修改 |
|------|------|
| `src/api/models/db_models.py` | Message 添加 confirmed_as 字段 |
| `src/api/services/conversation_service.py` | 新增 confirm_message + generate_outline_from_conv，get_conversation 返回 confirmed_as |
| `src/api/routes/conversations.py` | 新增 POST confirm + POST generate-outline 端点 |
| `frontend/src/views/Conversation.vue` | AI 回复添加确认按钮，结束后添加"生成总纲→卷纲"按钮 |
| `alembic/versions/b0018a838516_add_message_confirmed_as.py` | 新迁移 |

## 实现要点

- 每条 AI 回复可标记为"确定为世界观/人物/故事线"
- 确定后直接写入对应数据表（不是建议，是确定）
- 对话结束后可一键"生成总纲→卷纲"
- generate-outline 自动调用 outline_service 的两个生成方法
