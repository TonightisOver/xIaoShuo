# 技术设计: 大纲体系重构

**变更 ID**: CHANGE-016  
**创建时间**: 2026-05-16

## 1. 设计概述

将大纲从单层 JSON 重构为三级结构化体系（总纲→卷纲→章纲），存储在独立的 outlines 表中。每级大纲可通过 LLM 自动生成或手动编辑，支持从人机交互对话中提取总纲。

## 2. 数据库设计

```sql
CREATE TABLE outlines (
    id SERIAL PRIMARY KEY,
    novel_id VARCHAR(100) REFERENCES novels(novel_id) ON DELETE CASCADE,
    level VARCHAR(20) NOT NULL,  -- 'master' | 'volume' | 'chapter'
    volume_number INTEGER,       -- NULL for master
    chapter_number INTEGER,      -- NULL for master/volume
    content JSON NOT NULL,
    status VARCHAR(20) DEFAULT 'draft',  -- 'draft' | 'confirmed'
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX ix_outlines_novel_id ON outlines(novel_id);
CREATE UNIQUE INDEX ix_outlines_unique ON outlines(novel_id, level, COALESCE(volume_number, 0), COALESCE(chapter_number, 0));
```

### 总纲 content 结构
```json
{
  "premise": "核心前提",
  "main_conflict": "主要冲突",
  "plot_arcs": [
    {"name": "主线", "description": "...", "stages": ["起", "承", "转", "合"]}
  ],
  "ending": "结局走向",
  "themes": ["成长", "友情"]
}
```

### 卷纲 content 结构
```json
{
  "title": "卷标题",
  "summary": "本卷概要",
  "goal": "本卷阶段性目标",
  "climax": "本卷高潮",
  "chapters": [
    {"chapter": 1, "title": "...", "summary": "..."}
  ]
}
```

### 章纲 content 结构
```json
{
  "title": "章标题",
  "scenes": [
    {"location": "地点", "characters": ["人物"], "event": "事件"}
  ],
  "turning_point": "转折点",
  "emotional_beat": "情感节奏",
  "word_target": 5000
}
```

## 3. 服务层设计

### OutlineService

```python
class OutlineService:
    async def get_master_outline(novel_id) -> dict | None
    async def upsert_master_outline(novel_id, content) -> None
    async def get_volume_outline(novel_id, vol_num) -> dict | None
    async def get_chapter_outline(novel_id, ch_num) -> dict | None
    async def get_outline_tree(novel_id) -> dict  # 完整树形结构
    
    async def generate_volume_outlines(novel_id) -> list  # LLM 基于总纲生成
    async def generate_chapter_outlines(novel_id, vol_num) -> list  # LLM 基于卷纲生成
    async def generate_master_from_conversation(novel_id, conv_id) -> dict  # 从对话提取
```

### LLM Prompt 策略

**生成卷纲 prompt**:
```
你是小说大纲师。基于以下总纲，将故事拆分为 {num_volumes} 卷。

总纲：{master_outline}
目标字数：{target_words}
小说类型：{novel_type}

每卷要求：
- 有独立的阶段性目标和小高潮
- 前后卷情节连贯
- 字数分配合理

输出 JSON 数组...
```

**从对话生成总纲 prompt**:
```
分析以下创作对话，提取并整理为结构化的小说总纲。

对话内容：{conversation_text}
已有设定：{world_setting + characters}

输出 JSON 格式的总纲...
```

## 4. 前端设计

### 大纲 Tab（NovelDetail 新增）

```
大纲 Tab:
├── [从对话生成总纲] 按钮
├── 总纲 (可编辑)
│   ├── 核心前提: ...
│   ├── 主要冲突: ...
│   ├── 情节线: 主线 / 副线
│   └── 结局: ...
│   └── [确认总纲] [生成卷纲]
├── 卷纲列表
│   ├── 卷一: ... [编辑] [生成章纲]
│   ├── 卷二: ... [编辑] [生成章纲]
│   └── ...
└── 章纲（折叠在卷下）
    ├── 第1章: 场景/转折/情感
    └── ...
```

### 新增页面

- `frontend/src/views/OutlineEditor.vue` — 大纲编辑页（树形展示 + 编辑）

## 5. 修改文件清单

| 文件 | 修改 |
|------|------|
| `src/api/models/db_models.py` | 新增 Outline 模型 |
| `src/api/services/outline_service.py` | 新建，大纲 CRUD + LLM 生成 |
| `src/api/routes/outlines.py` | 新建，大纲 API 端点 |
| `src/api/routes/__init__.py` | 注册 outlines_router |
| `src/api/main.py` | 注册路由 |
| `frontend/src/views/NovelDetail.vue` | 添加"大纲" Tab |
| `frontend/src/views/OutlineEditor.vue` | 新建，大纲编辑页 |
| `frontend/src/router/index.js` | 添加大纲路由 |
| `alembic/versions/xxx.py` | 新迁移 |

## 6. 与现有系统的关系

- `volumes` 表保留，卷纲生成时同步更新 volumes 表
- `chapter_generation` 节点改为从 outlines 表读取章纲
- 现有"一键生成"流程保留兼容（自动走 总纲→卷纲→章纲→生成）
