# 集成测试报告: Web API (FastAPI)

**变更 ID**: CHANGE-003  
**版本**: v1  
**创建时间**: 2026-05-16

## 测试范围

端到端验证 API 请求 → 路由 → 服务 → 数据存储 → 响应的完整链路。

## 测试场景

| 场景 | 步骤 | 结果 |
|------|------|------|
| 创建并查询任务 | POST 创建 → GET 查询 | ✅ task_id 一致，状态正确 |
| 列表分页 | 创建多个任务 → GET 带 limit/offset | ✅ 分页参数生效 |
| 错误处理 | 发送无效请求 → 验证错误码 | ✅ 400/404/422 正确返回 |

## 测试工具

- httpx AsyncClient + ASGITransport（进程内测试，无需启动服务器）
- pytest-asyncio（异步测试支持）

## 结论

✅ 集成测试通过，API 端到端链路正常。
