# 技术设计: 功能修复与集成检查

**变更 ID**: CHANGE-019  
**创建时间**: 2026-05-17

## 1. 修复方案

### 1.1 对话结论应用修复

**问题**: apply-suggestion 端点逻辑可能有问题

**修复**:
- world 类型：读取当前世界观，将建议追加到 background 或 rules（根据内容判断）
- character 类型：从建议内容中提取人物名称和描述，创建完整人物记录
- 添加返回值确认（返回修改后的数据）

### 1.2 删除功能

**后端**:
- 添加 DELETE /api/v1/projects/{id}/chapters/{num} 端点
- 确认 DELETE /api/v1/projects/{id} 已存在（已有）

**前端**:
- ChapterEdit.vue 或章节列表添加删除按钮
- Home.vue 书架卡片添加删除按钮（带确认）
- NovelDetail 添加删除小说按钮

### 1.3 从对话生成故事线

**后端**:
- 新增 storyline_service 方法: generate_from_conversation
- LLM prompt 分析对话，提取故事线结构
- 新增 API: POST /projects/{id}/storylines/from-conversation/{conv_id}

**前端**:
- Conversation.vue 结论区域添加"生成故事线"按钮

### 1.4 生成内容与设定匹配

**修复 novel_generator.py**:
```python
# 在 generate_novel_background 中，读取完整设定注入 state
novel_manager = get_novel_manager()
world = await novel_manager.get_world_setting(novel_id) if novel_id else None
characters = await novel_manager.list_characters(novel_id) if novel_id else []
storylines = await storyline_service.list_storylines(novel_id) if novel_id else []

initial_state["world_setting"] = world
initial_state["characters"] = characters
# 将故事线摘要注入到 idea 中作为上下文
```

**修复各节点 prompt**:
- world_building: 如果已有世界观，prompt 改为"基于以下已有设定扩展"
- character_design: 如果已有人物，prompt 改为"基于以下已有人物补充"
- chapter_generation: prompt 注入故事线关键事件

### 1.5 集成检查

逐一验证：
- GET /projects → 列表正常
- GET /projects/{id} → 详情含所有字段
- GET /projects/{id}/world → 世界观
- GET /projects/{id}/characters → 人物列表
- GET /projects/{id}/chapters → 章节列表
- GET /projects/{id}/volumes → 卷列表
- GET /projects/{id}/outlines → 大纲树
- GET /projects/{id}/storylines → 故事线
- GET /projects/{id}/relations → 三元关系
- GET /projects/{id}/conversations → 对话列表
- WebSocket /ws/tasks/{id} → 连接正常

## 2. 修改文件

| 文件 | 修改 |
|------|------|
| `src/api/routes/conversations.py` | 修复 apply-suggestion 逻辑 |
| `src/api/routes/projects.py` | 添加 DELETE chapters 端点 |
| `src/api/services/novel_manager.py` | 添加 delete_chapter 方法 |
| `src/api/services/storyline_service.py` | 添加 generate_from_conversation |
| `src/api/routes/storylines.py` | 添加 from-conversation 端点 |
| `src/api/services/novel_generator.py` | 注入完整设定到 initial_state |
| `src/core/langgraph/nodes/world_building.py` | prompt 支持已有设定 |
| `src/core/langgraph/nodes/character_design.py` | prompt 支持已有人物 |
| `src/core/langgraph/nodes/chapter_generation.py` | prompt 注入故事线 |
| `frontend/src/views/Home.vue` | 添加删除小说按钮 |
| `frontend/src/views/NovelDetail.vue` | 添加删除小说/章节按钮 |
| `frontend/src/views/Conversation.vue` | 添加"生成故事线"按钮 |
