# 单元测试报告 — CHANGE-030 知识图谱

## 测试文件

`tests/unit/test_knowledge_graph_service.py`

## 执行结果

```
22 passed in 1.15s
```

## 覆盖率

| 模块 | 语句 | 未覆盖 | 覆盖率 |
|------|------|--------|--------|
| `src/api/services/knowledge_graph_service.py` | 217 | 56 | 74% |

未覆盖行主要为私有 DB 查询方法（`_get_triples_by_names`, `_get_hanging_foreshadowings`, `_get_recent_events`, `_build_history_context`, `_llm_consistency_check`），这些方法通过集成测试覆盖。

## 测试用例清单

### TestExtractFromChapter (7 cases)

| 用例 | 验证点 |
|------|--------|
| `test_feature_flag_disabled_returns_zero` | flag=False 时立即返回零计数 |
| `test_extract_creates_new_entities` | LLM 返回新实体正确写入 |
| `test_extract_deduplicates_existing_entity` | 已有实体不重复创建，更新 last_chapter |
| `test_extract_alias_match` | 别名匹配复用已有实体 ID |
| `test_extract_skips_empty_name` | 空名称实体被跳过 |
| `test_extract_skips_triple_with_unknown_entity` | 引用未知实体的三元组被跳过 |
| `test_extract_handles_llm_failure` | LLM 异常被捕获，返回 error 字段 |

### TestRetrieveContext (6 cases)

| 用例 | 验证点 |
|------|--------|
| `test_feature_flag_disabled_returns_empty` | flag=False 返回空字符串 |
| `test_no_matching_entities_returns_empty` | 大纲无匹配实体时返回空 |
| `test_matching_entities_returns_formatted_context` | 匹配实体时返回格式化上下文 |
| `test_context_truncated_at_1500_chars` | 超长上下文被截断到 1500 字符 |
| `test_retrieve_context_includes_foreshadowings` | 悬挂伏笔包含在上下文中 |
| `test_retrieve_context_exception_returns_empty` | 异常时优雅降级返回空 |

### TestCheckConsistency (6 cases)

| 用例 | 验证点 |
|------|--------|
| `test_feature_flag_disabled_returns_empty` | flag=False 返回空列表 |
| `test_dead_character_active_detected` | 死亡角色活动被规则检测 |
| `test_no_conflicts_returns_empty` | 无冲突时 LLM 不被调用 |
| `test_rule_conflict_triggers_llm_check` | 规则冲突触发 LLM 二次检查 |
| `test_consistency_check_exception_returns_empty` | 异常时优雅降级 |
| `test_non_action_predicate_not_flagged` | 非动作谓词不触发死亡角色告警 |

### TestFormatContext (3 cases)

| 用例 | 验证点 |
|------|--------|
| `test_empty_inputs_returns_header_only` | 空输入仅返回标题 |
| `test_includes_character_status` | 人物状态正确包含 |
| `test_includes_foreshadowing` | 伏笔信息正确包含 |

## Mock 策略

- `get_settings()` — 控制 feature flag
- `get_llm_client().generate()` — 返回预设 JSON 响应
- `get_db_session()` — mock session 避免真实 DB 连接
- 服务私有方法 — 直接替换为 AsyncMock（retrieve_context/check_consistency 测试）
