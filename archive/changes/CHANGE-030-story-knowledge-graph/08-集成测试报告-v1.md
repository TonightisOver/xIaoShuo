# 集成测试报告 — CHANGE-030 知识图谱

## 测试文件

`tests/integration/test_knowledge_graph_api.py`

## 执行结果

```
17 passed in 9.55s
```

## 综合覆盖率（单元 + 集成）

| 模块 | 语句 | 未覆盖 | 覆盖率 |
|------|------|--------|--------|
| `src/api/routes/knowledge_graph.py` | 186 | 28 | **85%** |
| `src/api/services/knowledge_graph_service.py` | 217 | 50 | **77%** |
| **合计** | **403** | **78** | **81%** |

覆盖率目标 > 80%：**达标**

## 测试用例清单

### TestEntityCRUD (7 cases)

| 用例 | 端点 | 验证点 |
|------|------|--------|
| `test_create_entity` | POST /entities | 201 + 正确字段返回 |
| `test_list_entities` | GET /entities | 列表包含已创建实体 |
| `test_list_entities_filter_by_type` | GET /entities?entity_type= | 类型过滤生效 |
| `test_get_entity_by_id` | GET /entities/{id} | 单个实体详情 |
| `test_get_entity_not_found` | GET /entities/{id} | 404 |
| `test_update_entity` | PUT /entities/{id} | 字段更新生效 |
| `test_delete_entity` | DELETE /entities/{id} | 204 + 后续 GET 404 |

### TestTripleCRUD (4 cases)

| 用例 | 端点 | 验证点 |
|------|------|--------|
| `test_create_and_list_triple` | POST + GET /triples | 创建后可列出 |
| `test_update_triple` | PUT /triples/{id} | 字段更新 |
| `test_delete_triple_marks_deleted` | DELETE /triples/{id} | 软删除，列表不可见 |
| `test_delete_triple_not_found` | DELETE /triples/{id} | 404 |

### TestExtraction (3 cases)

| 用例 | 端点 | 验证点 |
|------|------|--------|
| `test_extract_chapter_not_found` | POST /extract/{ch} | 章节不存在 404 |
| `test_extract_chapter_success` | POST /extract/{ch} | Mock LLM，验证抽取结果 |
| `test_extraction_logs` | GET /extraction-logs | 日志列表返回 |

### TestVisualization (1 case)

| 用例 | 端点 | 验证点 |
|------|------|--------|
| `test_visualization_returns_nodes_and_edges` | GET /visualization | nodes/edges 结构正确 |

### TestConsistencyCheck (2 cases)

| 用例 | 端点 | 验证点 |
|------|------|--------|
| `test_consistency_check_chapter_not_found` | POST /consistency-check/{ch} | 404 |
| `test_consistency_check_no_conflicts` | POST /consistency-check/{ch} | 无冲突返回空列表 |

## 测试环境

- 数据库：PostgreSQL (test instance, `tests/conftest.py` 配置)
- LLM：通过 `unittest.mock.patch` mock `get_llm_client`
- 后台任务：mock `generate_novel_background` / `generate_novel_full_background`
- HTTP 客户端：`httpx.AsyncClient` + `ASGITransport`

## 未覆盖路径说明

| 行范围 | 功能 | 原因 |
|--------|------|------|
| routes L330-365 (`extract_all`) | 全量重新抽取 | 需要多章节数据 + mock LLM，属于端到端场景 |
| routes L101, L168 等 | 部分过滤分支 | `name` 过滤、`entity_type` 更新的 None 分支 |
| service L362-407 | `_get_triples_by_names` | 通过 retrieve_context 间接测试（mock 了该方法） |
| service L499-538 | `_build_history_context` + `_llm_consistency_check` | 通过 check_consistency 间接测试（mock 了该方法） |

这些路径在真实 E2E 场景中会被覆盖，当前覆盖率已满足 > 80% 目标。
