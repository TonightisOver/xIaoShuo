# 编码计划

## 变更编号
CHANGE-005

## 实施步骤

### Step 1：实现事件总线
- 新建 `src/api/services/progress_event_bus.py`
- 实现 subscribe / unsubscribe / publish 接口
- 每个订阅者一个 asyncio.Queue
- 验证：单元测试 pub/sub 逻辑

### Step 2：实现 WebSocket 端点
- 新建 `src/api/routes/ws.py`
- 端点 `/ws/tasks/{task_id}`
- 连接时订阅，断开时清理
- 30 秒心跳 ping
- 验证：手动连接测试

### Step 3：改造生成流程
- `novel_generator.py`：ainvoke → astream
- 在流式迭代中发布 stage_start / stage_complete 事件
- 验证：现有测试仍通过

### Step 4：添加章节回调
- `chapter_generation.py`：注册回调函数
- 回调中发布 chapter_progress 事件
- 验证：生成过程中事件正确发布

### Step 5：路由注册
- 修改 `src/api/routes/__init__.py` 注册 ws router
- 修改 `src/api/main.py` 挂载 WebSocket 路由
- 验证：应用启动无报错

### Step 6：编写测试
- 新建 `tests/api/test_websocket.py`
- 4 个测试用例覆盖连接、事件接收、心跳、断开清理
- 验证：13 个测试全部通过
