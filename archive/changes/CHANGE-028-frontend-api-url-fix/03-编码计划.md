# 阶段 3: 编码计划

## 变更概述

修复 `frontend/src/views/NovelDetail.vue` 中两条 API URL 前缀错误。

## 任务拆解

| # | 任务 | 文件 | 操作 |
|---|------|------|------|
| 1 | 验证 NovelDetail.vue 第 342 行 URL | `frontend/src/views/NovelDetail.vue` | 读取确认 |
| 2 | 验证 NovelDetail.vue 第 343 行 URL | `frontend/src/views/NovelDetail.vue` | 读取确认 |
| 3 | 全局扫描其他 .vue 文件无同类问题 | `frontend/src/views/*.vue` | 扫描确认 |
| 4 | 运行后端静态检查（无 Python 改动） | `src/` | ruff + mypy |

## 验收标准

- 第 342 行: `fetch(\`/api/v1/projects/${novelId}/relations\`)`
- 第 343 行: `fetch(\`/api/v1/projects/${novelId}/outlines\`)`
- 全局 17 个 .vue 文件无其他错误前缀
- ruff/mypy 结果与改动前一致（后端无变更）

## 涉及文件

- `frontend/src/views/NovelDetail.vue` — 验证目标（预修复已正确，无需改动）

## 备注

技术设计已确认预修复正确，本阶段以验证为主，无实际代码写入。
