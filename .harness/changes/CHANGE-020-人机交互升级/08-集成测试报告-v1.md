# 集成测试报告: 人机交互升级

**变更 ID**: CHANGE-020  
**版本**: v1  
**执行时间**: 2026-05-17

## 测试场景

### 场景 1: 完整确认流程

```
1. 创建对话 → 发送消息 → AI 回复
2. POST /confirm {"confirm_as": "world"} → 200
3. 验证 messages.confirmed_as = "world"
4. 验证 world 表新增对应记录
5. GET 对话详情 → 消息包含 confirmed_as 字段
```

**结果**: ✅ 通过

### 场景 2: 生成总纲+卷纲流程

```
1. 创建对话 → 多轮讨论 → 确认若干设定
2. POST /generate-outline → 200
3. 验证 outlines 表新增 master outline
4. 验证自动生成 volume outlines
5. 返回结构包含 master_outline + volumes_generated: true
```

**结果**: ✅ 通过

### 场景 3: 前后端联调

| 操作 | 预期 | 结果 |
|------|------|------|
| AI 回复渲染 | 显示确认按钮 | ✅ |
| 点击"确定为世界观" | 调用 confirm API，按钮禁用，显示 badge | ✅ |
| 对话结束 | 显示"生成总纲→卷纲"按钮 | ✅ |
| 点击生成 | loading → 跳转大纲页 | ✅ |

### 场景 4: 数据库迁移兼容

| 验证项 | 结果 |
|--------|------|
| 迁移执行无报错 | ✅ |
| 已有消息 confirmed_as 为 null | ✅ |
| 新消息可正常写入 confirmed_as | ✅ |

## 结论

✅ 集成测试全部通过，端到端流程正常。
