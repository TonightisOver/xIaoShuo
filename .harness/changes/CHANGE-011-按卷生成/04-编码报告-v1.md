# 编码报告: 按卷生成

**变更 ID**: CHANGE-011  
**版本**: v1  
**创建时间**: 2026-05-16

## 修改文件

| 文件 | 修改 |
|------|------|
| `src/api/models/db_models.py` | 新增 Volume 模型，Chapter 添加 volume_number，Novel 添加 volumes 关系 |
| `src/core/langgraph/state.py` | 添加 volumes 字段 |
| `src/core/llm/prompts.py` | OUTLINE_GENERATION_PROMPT 要求输出卷结构 |
| `src/core/langgraph/nodes/outline_generation.py` | 解析 volumes，从中提取 chapter_outlines |
| `src/api/services/novel_manager.py` | 添加 volume CRUD（list/get/create/update） |
| `src/api/services/novel_generator.py` | 新增 generate_volume_background，_persist_to_novel 保存 volumes |
| `src/api/routes/projects.py` | 新增 volume 端点（list/get/put/generate-volume） |
| `alembic/versions/2001dedede3d_add_volumes_table.py` | volumes 表 + chapters.volume_number |

## 关键实现

1. **大纲输出格式变更**: prompt 要求 LLM 输出 volumes 数组，每卷含 chapters
2. **兼容旧格式**: 如果 LLM 返回旧格式（chapter_outlines），仍然正常工作
3. **按卷生成**: generate_volume_background 只生成指定卷的章节，前面卷的内容作为上下文
4. **上下文传递**: 前面已完成卷的最后一章结尾（500字）注入到当前卷第一章的 prompt
