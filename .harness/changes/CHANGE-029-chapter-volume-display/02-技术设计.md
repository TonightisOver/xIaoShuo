# CHANGE-029 技术设计

## 1. 设计原则

- 最小改动：只修复两个 bug，不重构数据模型
- 向后兼容：历史数据（`volume_number = NULL`）继续正常显示
- 不改变 URL 路由结构（`/novels/:id/chapters/:num`）

---

## 2. 核心决策：`chapter_number` 语义统一为全局编号

**现状**：`chapter_number` 在 DB 中设计为全局唯一编号（跨卷连续），但 `generate_volume_background` 写入时可能使用卷内编号，导致语义混乱。

**决策**：统一 `chapter_number` 为**全局唯一编号**。路由 `:num` 传递全局编号，后端按全局编号查询。前端显示时，用卷内序号（`chapter_number - vol.chapter_start + 1`）展示给用户。

这样：
- 路由唯一性得到保证（不同卷的章节有不同 URL）
- 后端查询逻辑不变（已经是按 `chapter_number` 查）
- 前端显示"第N章"时用卷内序号，符合用户预期

---

## 3. 修改方案

### 修改 1：`novel_manager.py` — `list_chapters` 补充 `volume_number` 字段

**文件**：`src/api/services/novel_manager.py`
**位置**：第 244-248 行

**现状**：
```python
return [{"id": c.id, "chapter_number": c.chapter_number,
         "title": c.title, "content": c.content,
         "word_count": c.word_count, "status": c.status,
         "updated_at": c.updated_at}
        for c in result.scalars().all()]
```

**修改后**：
```python
return [{"id": c.id, "chapter_number": c.chapter_number,
         "volume_number": c.volume_number,
         "title": c.title, "content": c.content,
         "word_count": c.word_count, "status": c.status,
         "updated_at": c.updated_at}
        for c in result.scalars().all()]
```

**说明**：增加 `"volume_number": c.volume_number` 一行。这是问题 1 的根本修复，前端 `VolumeList.volumeChapters()` 的过滤逻辑依赖此字段。

---

### 修改 2：`novel_generator.py` — 全量生成时补充 `volume_number`

**文件**：`src/api/services/novel_generator.py`
**位置**：第 916-935 行（`persist_results` 函数中的 Chapters 写入段）

**现状**：
```python
chapters = result.get("chapters", [])
async with get_db_session() as session:
    for ch in chapters:
        if isinstance(ch, dict):
            chapter = Chapter(
                novel_id=novel_id,
                chapter_number=ch.get("chapter", 0),
                title=ch.get("title", ""),
                content=ch.get("content", ""),
                word_count=ch.get("word_count", 0),
                status="generated",
                updated_at=datetime.now(timezone.utc),
            )
            session.add(chapter)
```

**修改后**：
```python
chapters = result.get("chapters", [])
async with get_db_session() as session:
    for ch in chapters:
        if isinstance(ch, dict):
            chapter = Chapter(
                novel_id=novel_id,
                volume_number=ch.get("volume_number"),
                chapter_number=ch.get("chapter", 0),
                title=ch.get("title", ""),
                content=ch.get("content", ""),
                word_count=ch.get("word_count", 0),
                status="generated",
                updated_at=datetime.now(timezone.utc),
            )
            session.add(chapter)
```

**说明**：增加 `volume_number=ch.get("volume_number")` 一行。如果 LLM 返回的章节数据中包含 `volume_number`，则正确写入；否则为 NULL（历史兼容）。

---

### 修改 3：`VolumeList.vue` — 修复章节显示逻辑

**文件**：`frontend/src/components/VolumeList.vue`

**3a. 修复 `volumeChapters` 过滤逻辑（第 43-46 行）**

现状已正确：`c.volume_number === vol.volume_number`。修改 1 补充了 `volume_number` 字段后，此处无需改动。

**3b. 修复章节显示的编号（第 24-27 行）**

**现状**：
```html
第{{ ch.chapter_number }}章：{{ ch.title }}
```

**修改后**：
```html
第{{ chapterIndexInVolume(vol, ch) }}章：{{ ch.title }}
```

在 `<script setup>` 中增加辅助函数：
```js
function chapterIndexInVolume(vol, ch) {
  // 如果卷有 chapter_start，用全局编号减去起始值得到卷内序号
  if (vol.chapter_start != null) {
    return ch.chapter_number - vol.chapter_start + 1
  }
  // 降级：按该卷章节列表中的排序位置
  const chs = props.chapters
    .filter(c => c.volume_number === vol.volume_number)
    .sort((a, b) => a.chapter_number - b.chapter_number)
  const idx = chs.findIndex(c => c.id === ch.id)
  return idx >= 0 ? idx + 1 : ch.chapter_number
}
```

**3c. 路由链接保持使用全局 `chapter_number`（第 22 行）**

```html
:to="`/novels/${novelId}/chapters/${ch.chapter_number}`"
```

此行**不变**。路由传全局编号，后端按全局编号查，保证唯一性。

---

### 修改 4：`ChapterEdit.vue` — 标题显示使用卷内序号（可选优化）

**文件**：`frontend/src/views/ChapterEdit.vue`
**位置**：第 8 行

**现状**：
```html
<h1 class="text-xl font-bold text-ink-900">第{{ chapter.chapter_number }}章：{{ chapter.title }}</h1>
```

**问题**：`chapter.chapter_number` 是全局编号，用户看到的是"第11章"而不是"第1章（第2卷）"。

**修改后**：后端 `get_chapter` 需要同时返回 `volume_number`，前端据此计算卷内序号。

**后端修改**（`novel_manager.py` 第 261-264 行）：

现状：
```python
return {"id": c.id, "chapter_number": c.chapter_number,
        "title": c.title, "content": c.content,
        "word_count": c.word_count, "status": c.status,
        "updated_at": c.updated_at}
```

修改后：
```python
return {"id": c.id, "chapter_number": c.chapter_number,
        "volume_number": c.volume_number,
        "title": c.title, "content": c.content,
        "word_count": c.word_count, "status": c.status,
        "updated_at": c.updated_at}
```

**前端修改**（`ChapterEdit.vue` 第 8 行）：
```html
<h1 class="text-xl font-bold text-ink-900">
  {{ chapter.volume_number ? `第${chapter.volume_number}卷 · ` : '' }}第{{ chapter.chapter_number }}章：{{ chapter.title }}
</h1>
```

注意：此处显示全局 `chapter_number` 即可（或加卷前缀），不需要计算卷内序号，因为 ChapterEdit 没有卷上下文数据。

---

### 修改 5：`NovelDetail.vue` — `unassignedChapters` 逻辑确认

**文件**：`frontend/src/views/NovelDetail.vue`
**位置**：第 311-313 行

```js
const unassignedChapters = computed(() =>
  chapters.value.filter(c => !c.volume_number)
)
```

修改 1 补充 `volume_number` 后，此处逻辑自动正确：`volume_number` 为 `null`/`undefined` 的章节归入未分卷区域。**无需改动**。

---

## 4. 数据流变更图

```
GET /api/v1/projects/:id/chapters
  novel_manager.list_chapters()
    [修改1] 返回包含 volume_number 的章节列表
      ↓
  NovelDetail.vue: chapters[]  (每项含 volume_number)
      ↓
  VolumeList.vue: volumeChapters(vol)
    filter(c => c.volume_number === vol.volume_number)  ← 现在能正确匹配
      ↓
  显示: 第{{ chapterIndexInVolume(vol, ch) }}章  ← 卷内序号
  链接: /novels/:id/chapters/:chapter_number     ← 全局编号

GET /api/v1/projects/:id/chapters/:num
  novel_manager.get_chapter(novel_id, chapter_number)
    [修改4] 返回包含 volume_number 的章节
      ↓
  ChapterEdit.vue: 显示 "第N卷 · 第M章：标题"
```

---

## 5. 修改文件汇总

| 文件 | 修改位置 | 改动量 |
|------|----------|--------|
| `src/api/services/novel_manager.py` | 第 244 行（list_chapters）、第 261 行（get_chapter） | +2 行 |
| `src/api/services/novel_generator.py` | 第 928 行（persist_results） | +1 行 |
| `frontend/src/components/VolumeList.vue` | 第 24-27 行（显示）、第 43-46 行后增加函数 | +10 行 |
| `frontend/src/views/ChapterEdit.vue` | 第 8 行（标题显示） | ~2 行 |

---

## 6. 不需要修改的文件

- `src/api/routes/projects.py`：路由逻辑正确，`chapter_number` 参数语义清晰
- `src/api/models/db_models.py`：数据模型已有 `volume_number` 字段，无需变更
- `frontend/src/views/NovelDetail.vue`：`unassignedChapters` 逻辑在修改 1 后自动正确

---

## 7. 风险与注意事项

1. **历史数据**：已生成的章节 `volume_number` 可能为 NULL（全量生成路径未写入）。修改 1 后这些章节会出现在"未分卷章节"区域，而不是卷内。这是正确的降级行为。

2. **`chapter_number` 语义**：如果 `generate_volume_background` 写入的是卷内编号（1、2、3...），则同一小说可能存在多条 `chapter_number = 1` 的记录，后端 `get_chapter` 的 `scalar_one_or_none()` 会抛出异常。需要在实现阶段验证实际数据，如有此情况需额外处理（如加 `volume_number` 联合查询条件）。

3. **`chapter_start` 字段**：`Volume` 表有 `chapter_start` 和 `chapter_end` 字段，但并非所有卷都有值（可能为 NULL）。`chapterIndexInVolume` 函数已处理降级逻辑。
