# 03 编码计划 — CHANGE-029

## 目标
补充 `volume_number` 字段的返回链路，修复前端卷内章节编号显示。

## 任务清单

| # | 文件 | 操作 | 验证方式 |
|---|------|------|----------|
| 1 | `src/api/services/novel_manager.py` | `list_chapters` 返回字典加 `"volume_number": c.volume_number` | 读取确认行号 |
| 2 | `src/api/services/novel_manager.py` | `get_chapter` 返回字典加 `"volume_number": c.volume_number` | 读取确认行号 |
| 3 | `src/api/services/novel_generator.py` | `persist_results` Chapter 构造加 `volume_number=ch.get("volume_number")` | 读取确认行号 |
| 4 | `frontend/src/components/VolumeList.vue` | 章节显示改用卷内序号，加 `chapterIndexInVolume()` 辅助函数 | 读取确认改动 |
| 5 | `frontend/src/views/ChapterEdit.vue` | 标题加卷前缀 `第N卷 · ` | 读取确认改动 |

## 前置确认（已完成）

- `Chapter` 模型 `volume_number` 字段：`db_models.py` 第 167 行，`Mapped[int | None]`，已存在
- `get_chapter` 查询条件：同时过滤 `novel_id` 和 `chapter_number`（第 253-255 行），无多条记录风险
- `list_chapters` 当前返回字典：第 244-248 行，缺少 `volume_number`
- `get_chapter` 当前返回字典：第 261-264 行，缺少 `volume_number`
- `persist_results` Chapter 构造：第 926-934 行，缺少 `volume_number`
- `VolumeList.vue`：第 25 行显示 `ch.chapter_number`，需改为卷内序号
- `ChapterEdit.vue`：第 8 行标题，需加卷前缀

## 执行顺序
按任务 1→2→3→4→5 顺序执行，后端先于前端。
