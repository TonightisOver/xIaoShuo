# 05 代码检查报告 v1 — CHANGE-029

## 检查命令

```
ruff check src/
mypy src/ --ignore-missing-imports
```

## ruff 结果

总计 113 个错误，全部为**预存问题**，本次改动未引入任何新问题。

主要预存问题类型：
- `E501` 行过长（88 字符限制），遍布多个文件
- `UP017` `datetime.UTC` 别名建议，集中在 `novel_manager.py` 和 `novel_generator.py`
- `F841`、`UP041`、`UP042`、`UP035` 等，均在未改动的文件或行

**本次新增行（novel_manager.py 第 245、263 行；novel_generator.py 第 927 行）均未触发任何 ruff 规则。**

## mypy 结果

总计 92 个错误（全项目），本次改动涉及的两个文件共 25 个错误，全部为**预存问题**：

- `novel_manager.py`：`no-untyped-def`（函数缺少类型注解），预存
- `novel_generator.py`：`call-overload`（LangGraph `astream` 重载不匹配）、`var-annotated`、`union-attr`、`index`，均预存

**本次新增行未引入任何新的 mypy 错误。**

## 前端文件

`VolumeList.vue` 和 `ChapterEdit.vue` 为 Vue 单文件组件，不在 ruff/mypy 检查范围内。已人工确认：
- `chapterIndexInVolume` 函数逻辑正确，`vol.chapter_start` 优先，回退到 `volume_number` 过滤
- `ChapterEdit.vue` 标题模板语法正确，`volume_number` 为 falsy 时不显示卷前缀

## 结论

本次改动质量门禁通过，无新增 lint/type 问题。
