# 04 编码报告 v1 — CHANGE-029

## 执行摘要

按技术设计完成全部 5 处改动，无额外修改。

## 改动明细

### 1. `src/api/services/novel_manager.py` — list_chapters（第 244 行）

在返回列表的字典中，`"chapter_number": c.chapter_number` 后加入：
```python
"volume_number": c.volume_number,
```

### 2. `src/api/services/novel_manager.py` — get_chapter（第 261 行）

在返回字典中，`"chapter_number": c.chapter_number` 后加入：
```python
"volume_number": c.volume_number,
```

### 3. `src/api/services/novel_generator.py` — persist_results Chapter 构造（第 926 行）

在 `chapter_number=ch.get("chapter", 0),` 前加入：
```python
volume_number=ch.get("volume_number"),
```

### 4. `frontend/src/components/VolumeList.vue`

- 第 25 行：`第{{ ch.chapter_number }}章` → `第{{ chapterIndexInVolume(vol, ch) }}章`
- `<script setup>` 中新增辅助函数 `chapterIndexInVolume(vol, ch)`，优先用 `vol.chapter_start` 换算，回退到按 `volume_number` 过滤后排序取索引

### 5. `frontend/src/views/ChapterEdit.vue`

- 第 8 行标题改为：
  ```html
  {{ chapter.volume_number ? `第${chapter.volume_number}卷 · ` : '' }}第{{ chapter.chapter_number }}章：{{ chapter.title }}
  ```

## 前置风险确认

`get_chapter` 查询同时过滤 `novel_id` 和 `chapter_number`（novel_manager.py 第 253-255 行），不存在多条记录风险。

## 未改动内容

- 路由链接 `:to` 保持使用 `ch.chapter_number`（全局编号）
- 测试文件未修改
- 无额外重构或格式调整
