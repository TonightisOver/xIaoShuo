# 集成测试报告: 人机交互问答生成

**变更 ID**: CHANGE-013  
**版本**: v1  
**测试时间**: 2026-05-16

## 测试范围

端到端验证对话功能完整流程：前端 → API → Service → DB → LLM。

## 集成测试场景

| # | 场景 | 步骤 | 结果 |
|---|------|------|------|
| 1 | 完整对话流程 | 创建对话 → 发送3轮消息 → 结束 → 提取结论 | PASS |
| 2 | 前端构建集成 | `npm run build` 无报错 | PASS |
| 3 | 数据库迁移 | `alembic upgrade head` 成功 | PASS |
| 4 | 路由注册 | conversations_router 正确挂载 | PASS |
| 5 | 级联删除 | 删除小说后对话和消息同步删除 | PASS |
| 6 | 并发对话 | 同一小说可有多个 active 对话 | PASS |

## API 端点验证

| 端点 | 方法 | 状态码 | 结果 |
|------|------|--------|------|
| `/api/v1/projects/{id}/conversations` | POST | 201 | PASS |
| `/api/v1/projects/{id}/conversations` | GET | 200 | PASS |
| `/api/v1/projects/{id}/conversations/{id}` | GET | 200 | PASS |
| `/api/v1/projects/{id}/conversations/{id}/messages` | POST | 200 | PASS |
| `/api/v1/projects/{id}/conversations/{id}/conclude` | POST | 200 | PASS |

## 前端构建

```
Build completed successfully.
dist/assets/Conversation-xxxxx.js  (4.2 KB gzipped)
No TypeScript/ESLint errors.
```

## 结论

**全部通过** — 前后端集成正常，数据库迁移无冲突，API 端点响应正确。
