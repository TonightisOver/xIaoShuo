# 部署验证报告: 人机交互问答生成

**变更 ID**: CHANGE-013  
**版本**: v1  
**验证时间**: 2026-05-16

## 部署清单

| 步骤 | 操作 | 状态 |
|------|------|------|
| 1 | 数据库迁移 `alembic upgrade head` | PASS |
| 2 | 后端服务重启 | PASS |
| 3 | 前端静态资源部署 | PASS |
| 4 | 环境变量确认 (DEEPSEEK_API_KEY) | PASS |

## 部署后验证

| 验证项 | 方法 | 结果 |
|--------|------|------|
| 新表存在 | `\dt conversations; \dt messages;` | PASS |
| 索引存在 | `\di ix_conversations_novel_id` | PASS |
| API 可达 | `GET /api/v1/projects/{id}/conversations` → 200 | PASS |
| 创建对话 | `POST /conversations` → 201 | PASS |
| 发送消息 | `POST /conversations/{id}/messages` → 200 | PASS |
| 结束对话 | `POST /conversations/{id}/conclude` → 200 | PASS |
| 前端页面 | 访问小说详情 → "创作对话" Tab 可见 | PASS |
| 聊天 UI | 发送消息 → AI 回复正常显示 | PASS |

## 回滚方案

1. 回滚代码到上一版本
2. 执行 `alembic downgrade -1` 删除 conversations/messages 表
3. 重启服务

## 影响范围

- 新增功能，不影响已有功能
- 新增 2 张表，无已有表结构变更
- 无破坏性变更

## 结论

**部署成功** — 所有验证项通过，功能正常运行。
