# 编码报告 v1

## 完成状态

全部 7 项修改已实现。

## 变更摘要

### H6 — 删除死代码配置项
- 从 `src/core/config.py` 删除 `TASK_STORAGE_PATH`、`TASK_MAX_CONCURRENT`、`TASK_CLEANUP_DAYS`

### C3 — 统一 structlog
- 7 个文件替换 `import logging` / `logging.getLogger` 为 `import structlog` / `structlog.get_logger`
- 修正 import 排序（stdlib → third-party → local）

### C1 — 抽取 `_build_initial_state`
- 新增 `_build_initial_state(task_id, request) -> tuple[dict, str | None]`
- `generate_novel_background` 和 `generate_novel_full_background` 均调用此函数
- 移除未使用的 `NovelState` import

### H4 — `generate_novel_background` 复用 `_run_langgraph_pipeline`
- 给 `_full_generate_percentage` 和 `_run_langgraph_pipeline` 增加 `total_stages` 参数
- `generate_novel_background` 调用 `_run_langgraph_pipeline(task_id, initial_state, stage_offset=0, total_stages=7)`
- 删除内联的 graph streaming 逻辑和 `_stage_percentage` 函数

### C2 — 抽取 `generate_single_chapter`
- 新增 `src/core/llm/chapter_generator.py`，包含 `generate_single_chapter()` 协程
- `chapter_generation.py` 节点循环体替换为调用此函数
- `generate_volume_background` 和 `generate_chapters_background` 循环体替换为调用此函数

### H5 — 子资源归属校验
- `update_power_system`、`delete_power_system`、`update_character`、`delete_character` 签名增加 `novel_id` 参数
- 查询条件增加 `novel_id` 过滤
- `projects.py` 路由层调用同步更新

### H7+H8 — 唯一约束
- `Chapter` 模型增加 `UniqueConstraint("novel_id", "chapter_number")`
- `Volume` 模型增加 `UniqueConstraint("novel_id", "volume_number")`
- 新增 alembic 迁移 `20260520_add_chapter_volume_unique.py`
