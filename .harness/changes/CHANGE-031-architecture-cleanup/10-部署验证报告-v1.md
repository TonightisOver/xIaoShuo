# 部署验证报告 — CHANGE-031

## 1. 应用启动验证

**方法**: 导入 FastAPI app 对象，验证模块加载链完整。

```
$ python -c "from src.api.main import app; print(type(app).__name__)"
FastAPI
```

**结果**: PASS — 应用模块树加载成功，无 ImportError。

## 2. 新模块导入验证

**目标**: `src/core/llm/chapter_generator.py`

```
$ python -c "from src.core.llm.chapter_generator import generate_single_chapter; print('OK')"
chapter_generator import OK
```

**导出函数**: `generate_single_chapter`（async）

**结果**: PASS — 新模块可正常导入，依赖链完整。

## 3. Alembic 迁移脚本验证

**文件**: `alembic/versions/20260520_add_chapter_volume_unique.py`

```
$ python -c "import ast; ast.parse(open('alembic/versions/20260520_add_chapter_volume_unique.py').read()); print('OK')"
alembic migration syntax OK
```

**验证内容**:
- Python 语法正确（AST 解析通过）
- revision / down_revision 链正确指向 `20260520_add_knowledge_graph_tables`
- upgrade(): 创建 `uq_chapter_novel_number` 和 `uq_volume_novel_number` 唯一约束
- downgrade(): 正确删除对应约束
- 与 db_models.py 中 `__table_args__` 定义的约束名一致

**结果**: PASS

## 4. 配置兼容性验证

**关注点**: 删除的配置项是否影响启动（.env 中可能仍有旧变量）

**分析**:
- `src/core/config.py` 使用 `pydantic-settings v2.14.1`
- `SettingsConfigDict` 未设置 `extra = "forbid"`
- pydantic-settings v2 默认行为：`extra = "ignore"`（忽略未声明的环境变量）
- 因此 .env 中残留的旧配置变量不会导致启动失败

**验证**:
```
$ python -c "from src.core.config import get_settings"
# 无报错（DEEPSEEK_API_KEY 未设置时延迟实例化不触发校验）
```

**结果**: PASS — 多余环境变量被安全忽略。

## 5. 数据库模型一致性

| 模型约束 | db_models.py | alembic 迁移 | 一致 |
|----------|-------------|-------------|------|
| `uq_chapter_novel_number` (novel_id, chapter_number) | Chapter.__table_args__ | upgrade() | YES |
| `uq_volume_novel_number` (novel_id, volume_number) | Volume.__table_args__ | upgrade() | YES |

## 总结

| 检查项 | 状态 |
|--------|------|
| 应用启动 | PASS |
| 新模块导入 | PASS |
| Alembic 迁移语法 | PASS |
| 配置兼容性 | PASS |
| 模型-迁移一致性 | PASS |

**部署验证通过。可安全部署。**
