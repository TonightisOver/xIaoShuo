# 编码计划: 数据隔离收口与跨小说负例测试

**阶段**: 3 - 编码计划  
**日期**: 2026-05-17

---

## 任务列表（按依赖顺序）

### T1: conversation_service.py — get_conversation 加 novel_id
- 修改签名：`get_conversation(self, conv_id: int, novel_id: str)`
- 查询加 `Conversation.novel_id == novel_id`
- 预计改动：~5 行

### T2: conversation_service.py — send_message/conclude_conversation 加 novel_id
- 依赖 T1
- `send_message(conv_id, content, novel_id)` — 传给 get_conversation
- `conclude_conversation(conv_id, novel_id)` — 传给 get_conversation
- 内部第二次 get_conversation 调用也传 novel_id
- 预计改动：~8 行

### T3: conversation_service.py — confirm_message 三元组校验
- 依赖 T1
- 查询改为 JOIN Conversation，同时限定 conv_id + novel_id
- 预计改动：~10 行

### T4: conversation_service.py — generate_outline_from_conv 内部调用同步
- 依赖 T1
- `get_conversation(conv_id, novel_id)` — novel_id 已有
- 预计改动：~2 行

### T5: conversations.py (routes) — 传 novel_id 给 service
- 依赖 T1-T4
- get_conversation、send_message、conclude_conversation 路由传 novel_id
- 预计改动：~6 行

### T6: storyline_service.py — update/delete arc/scene/storyline novel_id 必填
- 独立任务
- 6 个方法去掉 `= None`，查询条件改为无条件 AND
- 预计改动：~20 行

### T7: storyline_service.py — add_character_to_storyline 校验归属
- 依赖 T6
- 加 novel_id 参数，查询 Storyline 校验归属
- 预计改动：~10 行

### T8: storyline_service.py — get_relations 改 DB JOIN
- 独立任务
- StorylineCharacter JOIN Storyline WHERE novel_id
- 预计改动：~8 行

### T9: storyline_service.py — generate_from_conversation 内部调用同步
- 依赖 T1
- `conv_service.get_conversation(conv_id, novel_id)` — novel_id 已有
- 预计改动：~2 行

### T10: storylines.py (routes) — 传 novel_id 给 service
- 依赖 T6-T7
- update/delete arc/scene 传 novel_id；link_character 传 novel_id
- 预计改动：~8 行

### T11: tests/api/test_cross_novel_isolation.py — 跨小说负例测试
- 依赖 T1-T10
- 3 个测试：跨小说改 arc、跨小说改 scene、跨小说确认 message
- 预计新增：~80 行

---

## 文件清单

| 文件 | 操作 |
|------|------|
| `src/api/services/conversation_service.py` | 修改 |
| `src/api/routes/conversations.py` | 修改 |
| `src/api/services/storyline_service.py` | 修改 |
| `src/api/routes/storylines.py` | 修改 |
| `tests/api/test_cross_novel_isolation.py` | 新增 |

---

## 质量门禁

- ✅ 任务拆解合理，每个任务 < 50 行
- ✅ 依赖关系清晰，无循环依赖
- ✅ 文件清单完整，符合工程结构规范
- ✅ 优先级合理（service 层先于 route 层，route 层先于测试）
