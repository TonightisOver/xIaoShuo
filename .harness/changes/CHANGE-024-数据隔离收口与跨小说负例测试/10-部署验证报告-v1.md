# 部署验证报告 v1: 数据隔离收口与跨小说负例测试

**阶段**: 10 - 部署验证  
**日期**: 2026-05-17

---

## 部署验证

### 应用启动
`python -c "from src.api.main import app; print('App import OK')"` → ✅ App import OK

### 服务签名验证
```
get_conversation params: ['self', 'conv_id', 'novel_id']       ✅ novel_id 必填
send_message params: ['self', 'conv_id', 'content', 'novel_id'] ✅ novel_id 必填
confirm_message params: ['self', 'conv_id', 'msg_id', 'confirm_as', 'novel_id'] ✅ 三元组
update_character_arc params: ['self', 'arc_id', 'novel_id', 'kwargs'] ✅ novel_id 必填
add_character_to_storyline params: ['self', 'storyline_id', 'character_id', 'novel_id', 'role'] ✅ 归属校验
```

### 核心功能验证
- ✅ 跨小说改 arc → 404（测试通过）
- ✅ 跨小说改 scene → 404（测试通过）
- ✅ 跨小说确认 message → 404（测试通过）
- ✅ 正常创建/读取资源不受影响（59 passed）

---

## 变更文件清单

| 文件 | 变更类型 | 核心改动 |
|------|---------|---------|
| `src/api/services/conversation_service.py` | 修改 | get_conversation/send_message/conclude_conversation 加 novel_id；confirm_message JOIN 三元组 |
| `src/api/routes/conversations.py` | 修改 | 所有 conv 路由传 novel_id 给 service |
| `src/api/services/storyline_service.py` | 修改 | update/delete arc/scene/storyline novel_id 必填；add_character_to_storyline 归属校验；get_relations DB JOIN |
| `src/api/routes/storylines.py` | 修改 | update/delete arc/scene/link 路由传 novel_id |
| `src/api/services/outline_service.py` | 修改 | generate_master_from_conversation 内部调用同步 |
| `tests/api/test_cross_novel_isolation.py` | 新增 | 3 个跨小说负例测试 |

---

## 质量门禁

- ✅ 部署成功（应用正常启动）
- ✅ 核心功能可用（59 tests passed）
- ✅ 性能达标（无全表扫描，改为 DB JOIN）
