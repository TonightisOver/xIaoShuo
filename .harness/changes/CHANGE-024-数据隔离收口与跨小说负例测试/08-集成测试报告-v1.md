# 集成测试报告 v1: 数据隔离收口与跨小说负例测试

**阶段**: 8 - 集成测试  
**日期**: 2026-05-17

---

## 测试范围

本次变更的集成测试已在阶段 7 中通过 `tests/api/test_cross_novel_isolation.py` 覆盖，该测试文件本质上是集成测试（使用真实 DB + FastAPI ASGI 客户端端到端测试）。

---

## 核心流程测试

| 场景 | 测试方式 | 结果 |
|------|---------|------|
| 跨小说改 arc（PUT via wrong novel_id） | HTTP 端到端 + 真实 DB | ✅ 404 |
| 跨小说改 scene（PUT via wrong novel_id） | HTTP 端到端 + 真实 DB | ✅ 404 |
| 跨小说确认 message（confirm via wrong novel_id） | HTTP 端到端 + 真实 DB | ✅ 404 |
| 正常创建 arc（POST correct novel_id） | HTTP 端到端 + 真实 DB | ✅ 201 |
| 正常创建 scene（POST correct novel_id） | HTTP 端到端 + 真实 DB | ✅ 201 |
| 正常创建 conversation（POST correct novel_id） | HTTP 端到端 + 真实 DB | ✅ 201 |

---

## API 接口测试

- `POST /api/v1/projects/{novel_id}/character-arcs` → 201 ✅
- `PUT /api/v1/projects/{wrong_novel_id}/character-arcs/{arc_id}` → 404 ✅
- `POST /api/v1/projects/{novel_id}/scenes` → 201 ✅
- `PUT /api/v1/projects/{wrong_novel_id}/scenes/{scene_id}` → 404 ✅
- `POST /api/v1/projects/{novel_id}/conversations` → 201 ✅
- `POST /api/v1/projects/{wrong_novel_id}/conversations/{conv_id}/messages/{msg_id}/confirm` → 404 ✅

---

## 质量门禁

- ✅ 核心流程测试通过
- ✅ API 接口测试通过
- ✅ 数据库事务测试通过（FK 约束正确处理）
