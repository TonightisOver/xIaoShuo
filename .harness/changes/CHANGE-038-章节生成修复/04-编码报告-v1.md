# CHANGE-038 编码报告

> 日期：2026-05-21

## 变更摘要

修复章节生成超时、错误信息误导、人物/章节重复插入、章节缺少卷号等 6 个问题。

## 修改文件清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `src/core/llm/chapter_generator.py` | 修改 | 超时 300→600s，错误信息去除"API Key"误导 |
| `src/api/services/novel_manager.py` | 新增方法 | 新增 `get_character_by_name`；`get_chapter` 改为 order_by+limit(1) 防重复 500 |
| `src/api/services/novel_generator.py` | 修改 | `_persist_to_novel` 人物改为 upsert；章节改为先删后插；`generate_chapters_background` 新增 volume_number |
| `tests/unit/test_change038_dedup_and_timeout.py` | 新增 | 3 个单元测试覆盖超时值、错误信息、方法存在性 |

## 任务完成情况

- [x] T1 — CHAPTER_TIMEOUT_SECONDS 300→600
- [x] T2 — 超时错误信息不再提及 API Key
- [x] T3 — 人物 upsert（同名更新，不重复插入）
- [x] T4 — 章节先删后插（同 chapter_number 不重复）
- [x] T5 — `/relations` 路由已确认存在且返回格式正确
- [x] T6 — `generate_chapters_background` 写入章节时设置 volume_number

## 测试结果

```
tests/unit/test_change038_dedup_and_timeout.py: 3 passed
全量 unit 测试（不含 DB 依赖）: 57 passed, 0 failed
```
