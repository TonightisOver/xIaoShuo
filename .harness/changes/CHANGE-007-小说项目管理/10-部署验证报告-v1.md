# 部署验证报告 v1

**变更 ID**: CHANGE-007  
**阶段**: 10 - 部署验证  
**状态**: ✅ 通过

---

## 部署步骤

1. 执行 Alembic 迁移：`alembic upgrade head`
2. 重启应用服务
3. 验证端点可达

## 冒烟测试

| 测试场景 | 操作 | 预期 | 结果 |
|----------|------|------|------|
| 创建小说 | POST /projects | 201 + 返回 novel_id | ✅ |
| 编辑世界观 | PUT /projects/{id}/world | 200 + 内容更新 | ✅ |
| 添加角色 | POST /projects/{id}/characters | 201 | ✅ |
| 添加等级体系 | POST /projects/{id}/power-systems | 201 | ✅ |
| 查询章节 | GET /projects/{id}/chapters | 200 + 列表 | ✅ |
| 删除小说 | DELETE /projects/{id} | 200 + 子资源清除 | ✅ |

## 数据库验证

- 5 张新表已创建
- 外键约束生效
- tasks.novel_id 列已添加（nullable）
- 索引正常

## 回滚方案

如需回滚：
```bash
alembic downgrade -1
```
将移除 5 张新表和 novel_id 列，不影响现有 tasks 数据。

## 结论

部署成功，所有功能验证通过。小说项目管理功能可正常使用。
