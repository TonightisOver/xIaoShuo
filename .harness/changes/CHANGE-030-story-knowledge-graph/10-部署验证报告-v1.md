# 10 - 部署验证报告

## 变更: CHANGE-030-story-knowledge-graph
## 日期: 2026-05-20

---

## 1. 应用启动验证

```
> python -c "from src.api.main import app; print('OK')"
OK
```

结论: 应用可正常启动，所有模块导入无误。

## 2. 新模块导入验证

| 模块 | 导入命令 | 结果 |
|------|----------|------|
| `src.api.services.knowledge_graph_service` | `from src.api.services.knowledge_graph_service import get_knowledge_graph_service` | OK |
| `src.api.routes.knowledge_graph` | `from src.api.routes.knowledge_graph import router` | OK |
| `src.api.routes.__init__` | `knowledge_graph_router` 已注册 | OK |

路由注册确认:
- `src/api/routes/__init__.py` 导出 `knowledge_graph_router`
- `src/api/main.py` 通过 `app.include_router(knowledge_graph_router)` 注册
- 路由前缀: `/api/v1/projects/{novel_id}/knowledge-graph`
- 路由数量: 14 个端点

## 3. Alembic 迁移脚本验证

文件: `alembic/versions/20260520_add_knowledge_graph_tables.py`

| 检查项 | 结果 |
|--------|------|
| Python 语法 | PASS（`ast.parse` 通过） |
| revision ID | `20260520_add_knowledge_graph_tables` |
| down_revision | `20260519_add_status_indexes`（正确链接） |
| upgrade() | 创建 3 张表 + 8 个索引 |
| downgrade() | 按依赖顺序删除 3 张表 |

创建的表:
1. `knowledge_entities` — 实体表（含 novel_id 外键、3 个索引）
2. `knowledge_triples` — 三元组表（含 subject_id/object_id 外键、5 个索引）
3. `knowledge_extraction_logs` — 抽取日志表（含唯一索引）

外键约束: 所有外键设置 `ondelete="CASCADE"`，确保数据一致性。

## 4. Feature Flag 验证

配置项: `KNOWLEDGE_GRAPH_ENABLED` (bool, 默认 `True`)

```
> $env:KNOWLEDGE_GRAPH_ENABLED = "false"
> python -c "from src.core.config import get_settings; get_settings.cache_clear(); s = get_settings(); print(f'KNOWLEDGE_GRAPH_ENABLED={s.KNOWLEDGE_GRAPH_ENABLED}')"
KNOWLEDGE_GRAPH_ENABLED=False
```

Feature flag 控制点:
| 位置 | 行为（disabled 时） |
|------|---------------------|
| `knowledge_graph_service.extract_from_chapter()` | 返回 `{entities_count: 0, triples_count: 0}` |
| `knowledge_graph_service.retrieve_context()` | 返回空字符串 |
| `knowledge_graph_service.check_consistency()` | 返回空列表 |
| `chapter_generation.node()` | 跳过 KG 上下文检索 |
| `quality_check.node()` | 跳过一致性检查 |

结论: Feature flag 可通过环境变量 `KNOWLEDGE_GRAPH_ENABLED=false` 完全关闭知识图谱功能，不影响核心生成流程。

---

## 总结

| 验证项 | 状态 |
|--------|------|
| 应用启动 | PASS |
| 模块导入 | PASS |
| 迁移脚本语法 | PASS |
| 迁移脚本结构 | PASS（upgrade/downgrade 对称） |
| Feature Flag 环境变量控制 | PASS |

部署验证通过。部署前需执行 `alembic upgrade head` 创建知识图谱表。
