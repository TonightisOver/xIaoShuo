# 集成测试报告: 大纲体系重构

**变更 ID**: CHANGE-016  
**版本**: v1  
**测试时间**: 2026-05-17

## 测试范围

验证大纲体系端到端数据流：总纲编辑 → 卷纲生成 → 章纲生成；从对话生成总纲；前端组件与 API 交互。

## 集成测试用例

### 大纲 CRUD 全流程

| # | 场景 | 步骤 | 预期 | 结果 |
|---|------|------|------|------|
| 1 | 创建并读取总纲 | PUT master → GET master | 返回刚写入的 content | PASS |
| 2 | 更新总纲 | PUT master 两次 → GET master | 返回最新 content，仅一条记录 | PASS |
| 3 | 树形查询 | 写入 master + 2 卷 + 3 章 → GET /outlines | 返回完整树，chapters 按卷分组 | PASS |

### LLM 生成流程

| # | 场景 | 步骤 | 预期 | 结果 |
|---|------|------|------|------|
| 1 | 生成卷纲 | 先创建总纲 → POST /generate-volumes | 返回卷纲列表，DB 中写入对应记录 | PASS |
| 2 | 生成卷纲无总纲 | 直接 POST /generate-volumes | 400 "总纲不存在" | PASS |
| 3 | 生成章纲 | 先有卷纲 → POST /generate-chapters/1 | 返回章纲列表，DB 中写入记录 | PASS |
| 4 | 生成章纲无卷纲 | POST /generate-chapters/99 | 400 "卷99大纲不存在" | PASS |

### 从对话生成总纲

| # | 场景 | 步骤 | 预期 | 结果 |
|---|------|------|------|------|
| 1 | 有效对话 | 创建对话含消息 → POST /from-conversation/{id} | 返回结构化总纲 JSON | PASS |
| 2 | 对话不存在 | POST /from-conversation/999 | 400 "对话不存在" | PASS |

### 前端构建与路由

| # | 场景 | 验证点 | 结果 |
|---|------|--------|------|
| 1 | npm run build | 编译成功，OutlineEditor.vue 正确打包 | PASS |
| 2 | 路由注册 | /novels/:id/outlines 路由可访问 | PASS |
| 3 | NovelDetail 大纲 Tab | Tab 渲染，链接指向 OutlineEditor | PASS |

## 测试统计

| 指标 | 值 |
|------|-----|
| 总场景数 | 12 |
| 通过 | 12 |
| 失败 | 0 |
| 覆盖层 | DB 模型、Service、API 路由、前端组件 |

## 结论

大纲体系各层集成正常。CRUD 全流程、LLM 生成链路、从对话提取总纲、前端交互均符合预期。
