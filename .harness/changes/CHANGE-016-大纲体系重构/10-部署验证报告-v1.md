# 部署验证报告: 大纲体系重构

**变更 ID**: CHANGE-016  
**版本**: v1  
**创建时间**: 2026-05-17

## 部署方式

```bash
cd /opt/xiaoshuo && git stash push -u -m temp && git pull && docker compose up -d --build
```

## 验证项目

| 验证项 | 方法 | 结果 |
|--------|------|------|
| 数据库迁移 | alembic upgrade head | ✅ outlines 表创建 |
| 大纲树形 API | GET /api/v1/projects/{id}/outlines | ✅ 返回 master/volumes/chapters |
| 总纲编辑 | PUT /outlines/master | ✅ 保存成功 |
| 生成卷纲 | POST /outlines/generate-volumes | ✅ LLM 生成并存储 |
| 生成章纲 | POST /outlines/generate-chapters/{vol} | ✅ LLM 生成并存储 |
| 从对话生成 | POST /outlines/from-conversation/{id} | ✅ 提取结构化总纲 |
| 前端大纲编辑器 | /novels/{id}/outlines 页面 | ✅ 可编辑总纲、查看卷纲/章纲 |
| NovelDetail 大纲 Tab | 小说详情页 | ✅ 显示"大纲"Tab + 编辑入口 |
| 所有测试 | pytest | ✅ 13 passed |

## 回滚方案

```bash
git revert HEAD
docker compose up -d --build
# 回退迁移
docker compose exec api alembic downgrade -1
```

## 结论

✅ 部署验证通过。
