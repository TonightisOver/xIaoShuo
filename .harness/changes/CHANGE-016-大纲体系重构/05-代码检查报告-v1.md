# 代码检查报告: 大纲体系重构

**变更 ID**: CHANGE-016  
**版本**: v1  
**检查时间**: 2026-05-17

## 检查范围

后端 Python（Outline 模型 + OutlineService + outlines 路由）、前端 Vue（OutlineEditor.vue + NovelDetail 大纲 Tab）、数据库迁移。

## 检查结果

### 通过项

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | Outline 模型字段完整（novel_id/level/volume_number/chapter_number/content/status） | PASS |
| 2 | OutlineService CRUD 方法使用 upsert 模式，避免重复记录 | PASS |
| 3 | 3 个 LLM 生成方法（generate_volume_outlines/generate_chapter_outlines/generate_master_from_conversation）逻辑完整 | PASS |
| 4 | API 路由 RESTful 设计，GET/PUT/POST 语义正确 | PASS |
| 5 | get_outline_tree 返回树形结构，chapters 按 volume 分组 | PASS |
| 6 | 前端 OutlineEditor.vue 总纲编辑 + 卷纲列表 + 章纲折叠展示 | PASS |
| 7 | Pydantic 请求模型字段均为 Optional，支持部分更新 | PASS |
| 8 | 迁移文件正确创建 outlines 表及索引 | PASS |

### 发现问题

| # | 严重度 | 文件 | 问题 | 建议 |
|---|--------|------|------|------|
| 1 | 低 | `outlines.py` | update_chapter_outline 硬编码 volume_number=0 | 应从请求体或查询参数获取实际卷号 |
| 2 | 低 | `outline_service.py` | generate_volume_outlines 中 safe_json_parse 返回空列表时无日志 | 建议 LLM 返回非法 JSON 时记录 warning |
| 3 | 信息 | `OutlineEditor.vue` | load() 中调用了两次 GET /outlines（第二次用于获取 chapters） | 可合并为一次调用，tree 接口已包含 chapters |

### 代码风格

- 命名规范：Python snake_case，Vue 组件 PascalCase，符合项目约定
- 导入顺序：后端按标准库/第三方/本地分组，符合项目风格
- 类型注解：Outline 模型使用 Mapped 类型，service 方法有返回类型标注
- Prompt 模板：使用模块级常量，结构清晰

## 结论

代码质量良好，无阻塞性问题。3 个低优先级建议可后续迭代处理。
