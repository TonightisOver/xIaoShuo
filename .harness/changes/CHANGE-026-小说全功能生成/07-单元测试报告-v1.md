# 单元测试报告 — CHANGE-026-小说全功能生成

**日期**: 2026-05-18
**版本**: v1
**测试执行人**: Tester Agent (AI)

---

## 测试结果

| 指标 | 数值 |
|------|------|
| 通过 | 21 |
| 失败 | 0 |
| 跳过 | 0 |
| 总计 | 21 |

## 新增测试文件

| 文件 | 测试数 | 覆盖函数 |
|------|--------|---------|
| `tests/unit/test_change026_full_generate.py` | 21 | 4 个新函数 |

## 按函数覆盖详情

### `StorylineService.generate_power_systems_ai()`
| 测试 | 状态 | 场景 |
|------|------|------|
| `test_generates_power_systems_from_world_setting` | 通过 | 完整世界观下生成多个力量体系 |
| `test_generates_power_systems_with_empty_world` | 通过 | 无世界观仅用小说信息生成 |
| `test_raises_when_novel_not_found` | 通过 | 小说不存在时抛出 ValueError |
| `test_handles_llm_returning_invalid_json` | 通过 | LLM 返回无效 JSON 时降级为空列表 |
| `test_handles_llm_returning_singular_dict` | 通过 | LLM 返回 dict 而非 list 时降级 |
| `test_skips_items_without_name` | 通过 | 跳过缺少 name 字段的条目 |

### `OutlineService.persist_outlines_from_result()`
| 测试 | 状态 | 场景 |
|------|------|------|
| `test_persists_master_volumes_and_chapters` | 通过 | 完整的 master+卷+章持久化 |
| `test_persists_standalone_chapters_only` | 通过 | 仅独立章节无卷 |
| `test_persists_empty_result_gracefully` | 通过 | 空结果返回零计数 |
| `test_persists_volumes_without_chapters` | 通过 | 有卷无章 |
| `test_plot_arcs_fallback_not_dict` | 通过 | plot_arcs 回退（列表非 dict） |
| `test_skips_non_dict_chapters_in_volumes` | 通过 | 跳过非 dict 类型的章 |

### `ConversationService.generate_auto_conversation()`
| 测试 | 状态 | 场景 |
|------|------|------|
| `test_creates_conversation_with_ai_suggestion` | 通过 | 完整设定下创建 AI 自动对话 |
| `test_raises_when_novel_not_found` | 通过 | 小说不存在时抛出 ValueError |
| `test_handles_minimal_settings` | 通过 | 无世界观/无人物时正常降级 |

### `novel_generator._run_sub_feature()`
| 测试 | 状态 | 场景 |
|------|------|------|
| `test_sub_feature_power_systems` | 通过 | 分发到力量体系生成 |
| `test_sub_feature_skips_when_novel_id_is_none` | 通过 | novel_id=None 时跳过子功能 |
| `test_sub_feature_handles_error_gracefully` | 通过 | 异常时发布 ERROR 事件（non_blocking） |
| `test_sub_feature_percentage_calculation` | 通过 | 百分比计算验证 |

### `novel_generator._full_generate_percentage()`
| 测试 | 状态 | 场景 |
|------|------|------|
| `test_boundaries` | 通过 | 首尾边界和中间百分比值 |
| `test_monotonic` | 通过 | 百分比单调递增 |

## 覆盖率

| 文件 | 语句数 | 覆盖率 | 说明 |
|------|--------|--------|------|
| `src/api/services/storyline_service.py` | 243 | 23% | 仅覆盖 `generate_power_systems_ai()` |
| `src/api/services/outline_service.py` | 175 | 28% | 仅覆盖 `persist_outlines_from_result()` |
| `src/api/services/conversation_service.py` | 178 | 29% | 仅覆盖 `generate_auto_conversation()` |
| `src/api/services/novel_generator.py` | 337 | 12% | 仅覆盖 `_run_sub_feature()` 及辅助函数 |

**新增函数覆盖率**: 所有 4 个新增函数的核心逻辑路径均已覆盖（参数校验、空输入降级、异常容错、事件发布）。模块级覆盖率较低是因为单元测试仅针对 CHANGE-026 新增的函数，未触及模块已有的大段代码（如 `generate_novel_background`、`generate_volume_background` 等），这些在 CHANGE-009~023 中已有覆盖。

## 未覆盖区域

| 函数 | 未覆盖分支 | 原因 |
|------|-----------|------|
| `_run_sub_feature` outline_persist / storylines / character_arcs / scenes / auto_conversation 分支 | 仅测试了 power_systems 和 novel_id=None 路径 | 其余分支具有相同结构（服务调用 + 事件发布），已通过 power_systems 路径验证分发逻辑正确性 |
| `generate_auto_conversation` 空内容回退 | LLM 返回空字符串时无默认回退文本 | 此为空内容边界，已在专家评审报告 W4 中标注建议 |

## 测试技术说明

- 使用 `unittest.mock.AsyncMock` + `patch` 隔离 LLM 客户端、数据库会话和事件总线
- 测试文件遵循项目现有模式（参考 `tests/unit/test_change025_fixes.py`）
- 所有测试标记为 `@pytest.mark.asyncio` 支持异步执行
- 无真实数据库或网络调用
