# Gate 1 审批 - CHANGE-043 API压力测试扩展

## 审批时间
2026-05-25

## 需求摘要

扩展现有 Locust 压测框架 + 新增 k6 脚本，覆盖项目全部核心 API。

## 方案要点

1. **Locust 扩展**：从 4 个场景扩展到 ~18 个，按 Reader/Writer/Admin 三种用户画像组织
2. **k6 新增**：编写 k6 脚本用于 CI 自动化，内置阈值断言（P95 < 200ms 读 / < 500ms 写）
3. **Seed 数据**：自动从现有 20 章数据生成 seed.json
4. **双工具并行**：Locust 用于交互探索，k6 用于自动化回归

## 影响范围
- 新增/修改文件均在 `loadtest/` 目录
- 不影响业务代码
- 不影响数据库

## 待审批
- [ ] 方案确认
